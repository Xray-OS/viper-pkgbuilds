pkgname=arch7z-fish-config
_pkgname=arch7z-fish-config
_destname="/etc"
# _licensedir="/usr/share/ada/licenses/"
pkgver=1
pkgrel=2
pkgdesc="Fish settings for Arch7z"
arch=('x86_64')
url="https://gitlab.com/Arch7z/$_pkgname"
license=('GPL3')
makedepends=('git')
depends=()
#conflicts=('')
provides=("${pkgname}")
options=( !strip !emptydirs )
source=(${pkgname}::"git+https://gitlab.com/Arch7z/${_pkgname}")
sha256sums=('SKIP')
package() {
  install -dm755 "$pkgdir$_destname"
  cp -r "$srcdir/$pkgname/$_destname/"* "$pkgdir/$_destname"
}

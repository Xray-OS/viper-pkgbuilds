# Maintainer: Angel Gustavo Lopez xray_os@proton.me
pkgname=xray-grub-theme
_pkgname=xray-grub-theme
_destname1="/usr/share/grub/themes/xray_os"
_destname2="/boot/grub/themes/xray_os"
_licensedir="/usr/share/xray_os/licenses/"
pkgver=1
pkgrel=5
pkgdesc="Grub theme for Xray_OS"
arch=(any)
url="https://gitlab.com/xray_os-archlinux/$_pkgname"
license=('GPL3')
optdepends=('grub-customizer')
makedepends=('git')
depends=()
install=readme.install
conflicts=()
provides=("${pkgname}")
options=(!strip !emptydirs)
source=(${_pkgname}::git+${url})
md5sums=('SKIP')
package() {
	if [ -d "${pkgdir}${_licensedir}${_pkgname}" ]; then
	mkdir -p "${pkgdir}${_licensedir}${_pkgname}"
	mv "${srcdir}/${_pkgname}/"LICENSE "${pkgdir}${_licensedir}${_pkgname}/LICENSE"
	fi

	if [ -d "${pkgdir}${destname1}" ]; then
	mkdir -p "${pkgdir}${destname1}"
	cp -r "${srcdir}/${_pkgname}/${destname1}/"* "${pkgdir}${destname1}"
	fi

	if [ -d "${pkgdir}${destname2}" ]; then
	mkdir -p "${pkgdir}${destname2}"
	cp -r "${srcdir}/${_pkgname}/${destname2}/"* "${pkgdir}${destname2}"
	fi
}
